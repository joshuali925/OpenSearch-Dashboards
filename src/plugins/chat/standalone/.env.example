# Chat Standalone Server Configuration

# Server port
PORT=3001

# CORS origin (frontend URL)
CORS_ORIGIN=http://localhost:5173

# AG-UI Backend URL (required for chat functionality)
# Point this to your AG-UI server endpoint
AG_UI_URL=http://localhost:8000

# Alternatively, use ML Commons Agent (OpenSearch plugin)
# Uncomment and configure if using ML Commons instead of AG-UI
# ML_COMMONS_AGENT_ID=your-agent-id-here

# Note: You need to configure either AG_UI_URL or ML_COMMONS_AGENT_ID
# for the chat to work properly
